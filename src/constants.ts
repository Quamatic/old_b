/**
 * The maximum size (in bytes) that a buffer can allocate, which is equal to 1GB.
 */
export const MAX_BUFFER_SIZE = 0x40000000;

/** Size of a 8-bit value in bytes */
export const EIGHT_BIT_SIZE = 1;

/** Size of a 16-bit value in bytes  */
export const SIXTEEN_BIT_SIZE = 2;

/** Sie of a 32-bit value in bytes */
export const THIRTY_TWO_BIT_SIZE = 4;

/** Size of a 64-bit value in bytes */
export const SIXTY_FOUR_BIT_SIZE = 8;
